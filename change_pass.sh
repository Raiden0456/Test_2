# Changing root password
sudo passwd root
#Find all files with bitrix text in a folder
grep -r "bitrix" /home/
#Update an Ubuntu server
sudo apt-get update
sudo apt-get upgrade
#Optinal reboot
sudo reboot